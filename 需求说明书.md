# 智能扫地机器人维保客服系统 需求说明书

| 项目名称 | 智能扫地机器人维保客服系统 |
|---|---|
| 文档版本 | V1.0 |
| 编制日期 | 2026-08-26 |
| 文档状态 | 正式版 |
| 适用范围 | 需求评审、开发、测试与验收 |

---

## 1. 引言

### 1.1 编写目的

本文档描述智能扫地机器人维保客服系统的完整需求，包括系统定位、功能需求、非功能需求、数据需求、外部接口与验收标准，作为设计、开发、测试与验收的基线依据。

### 1.2 项目背景

随着扫地机器人产品的普及，传统人工客服难以应对高频、重复的维保咨询。本系统基于 LangGraph ReAct Agent 构建企业级智能客服平台，融合 RAG（检索增强生成）混合检索、多工具调用与 LLM-as-judge 评测体系，为扫地/扫拖机器人用户提供保养咨询、故障排查、选购建议、天气联动建议、个性化使用报告生成等服务，支持多用户登录与会话隔离，可通过 Docker 一键部署至服务器。

### 1.3 术语定义

| 术语 | 说明 |
|---|---|
| Agent | 基于大语言模型的智能体，具备自主思考、工具调用与决策能力 |
| ReAct | Reasoning + Acting，LLM 通过「思考→行动→观察→再思考」循环完成任务 |
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| BM25 | 经典关键词相关性排序算法（rank-bm25 实现） |
| RRF | Reciprocal Rank Fusion，倒数排名融合算法，用于多路检索结果融合 |
| Cross-encoder | 交叉编码器重排序模型，对查询-文档对进行精细相关性打分 |
| Chunk | 文档切分后的文本块 |
| Embedding | 文本向量化表示模型 |
| LLM-as-judge | 用大模型作为裁判对回答质量打分的评测方式 |
| runtime context | LangGraph 执行上下文，按次请求传递城市、用户 ID 等会话级数据 |
| Middleware | LangGraph Agent 中间件，可拦截工具调用、模型调用与提示词生成 |

### 1.4 参考资料

- README.md（项目说明文档）
- LangGraph / LangChain 官方文档
- 高德开放平台 API 文档（IP 定位、地理编码、天气查询）
- DeepSeek 开放平台 API 文档（OpenAI 兼容接口）

---

## 2. 总体描述

### 2.1 产品定位

面向扫地机器人厂商/经销商的企业级智能售后服务系统。用户通过 Web 界面与 AI 客服对话，获得基于知识库的精准维保指导与个性化服务，同时支持运营方通过评测体系持续度量与提升系统回答质量。

### 2.2 用户特征

| 用户角色 | 特征 | 使用场景 |
|---|---|---|
| 终端用户 | 扫地/扫拖机器人消费者，非技术人员 | 保养咨询、故障排查、选购建议、天气联动建议、个人使用报告 |
| 运营/客服人员 | 客服主管、质检人员 | 运行评测、查看知识库状态、维护知识文件 |

### 2.3 运行环境

| 项目 | 要求 |
|---|---|
| 操作系统 | Windows / macOS / Linux；Docker 部署时基于 python:3.12-slim |
| 运行时 | Python 3.12+ |
| 浏览器 | 支持 Streamlit 1.59 的现代浏览器（Chrome / Edge / Safari） |
| 网络 | 需可访问 DeepSeek API、高德地图 API；HuggingFace 模型本地缓存后支持离线 |
| 硬件 | 服务器需具备约 2GB+ 内存（含 Embedding 与 Reranker 模型加载） |

### 2.4 设计约束与假设

- 对话语言为中文，知识库内容以中文为主。
- LLM 使用 DeepSeek V4（OpenAI 兼容接口），通过环境变量 `DEEPSEEK_API_KEY` 提供密钥。
- 外部工具数据（高德天气/定位）依赖外网可用；不可用时系统优雅降级（返回错误提示或默认城市）。
- 多用户隔离通过 SQLite 数据表 `user_id` 外键约束实现。
- 系统以单机单容器部署为目标，不依赖独立数据库服务。

---

## 3. 系统总体架构

### 3.1 架构分层

```
┌─────────────────────────────────────────────────────┐
│  表现层  Streamlit Web 应用（登录/聊天/知识库/会话管理）│
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│  Agent 引擎  LangGraph ReAct Agent（create_agent）   │
│  ├─ 动态提示词（主提示词 / 报告提示词 自动切换）       │
│  ├─ 中间件：工具监控重试 / 模型调用日志 / 提示词切换   │
│  └─ 上下文管理：超窗 LLM 摘要压缩                     │
└───────┬──────────────────────────┬──────────────────┘
        ▼                          ▼
┌───────────────┐        ┌──────────────────────┐
│  工具层        │        │  RAG 检索引擎          │
│  rag_search   │        │  ChromaDB 向量检索     │
│  get_weather  │        │  BM25 关键词检索       │
│  get_user_*   │        │  RRF 融合             │
│  fetch_*      │        │  Cross-encoder 重排   │
└───────┬───────┘        └──────────┬───────────┘
        ▼                           ▼
┌─────────────────────────────────────────────────────┐
│  数据层  SQLite（用户/会话/消息/登录态）│ 文件系统（知识库/外部CSV）│
├─────────────────────────────────────────────────────┤
│  外部服务  高德地图（IP定位/地理编码/天气）│ DeepSeek API │ HF 模型 │
└─────────────────────────────────────────────────────┘
```

### 3.2 技术栈

| 层 | 技术 |
|---|---|
| Agent 框架 | LangGraph 1.2.9（`create_agent`，ReAct 循环） |
| LLM | DeepSeek V4（`deepseek-v4-flash`，OpenAI 兼容 API，ChatOpenAI） |
| Embedding | BAAI/bge-small-zh-v1.5（HuggingFaceEmbeddings，本地离线加载） |
| 向量数据库 | ChromaDB（collection: `agent`，持久化目录 `chroma_db/`） |
| 关键词检索 | BM25（rank-bm25，中文按字符切分） |
| 重排序 | BAAI/bge-reranker-v2-m3（CrossEncoder，可选降级） |
| 前端 | Streamlit 1.59.1 |
| 持久化 | SQLite（WAL 模式，单文件零部署） |
| 外部 API | 高德地图（IP 定位 / 地理编码 / 天气查询） |
| 部署 | Docker + Docker Compose |
| 日志 | logging.RotatingFileHandler（10MB × 10 轮转） |

### 3.3 模块划分

| 模块 | 主要文件 | 职责 |
|---|---|---|
| 前端应用 | `app.py` | 登录/注册/重置密码、聊天界面、知识库管理、会话管理、城市设置 |
| Agent 引擎 | `agent/react_agent.py` | ReAct Agent 构建、流式执行、上下文压缩 |
| 工具层 | `agent/tools/agent_tools.py` | 7 个业务工具定义 |
| 中间件 | `agent/tools/middleware.py` | 工具监控重试、模型日志、动态提示词切换、评测 trace |
| 混合检索 | `rag/hybrid_retriever.py` | BM25 + 向量 + RRF 融合 + Cross-encoder 重排 |
| 检索服务 | `rag/rag_service.py` | 检索服务封装、语料缓存、检索与生成解耦 |
| 向量存储 | `rag/vector_store.py` | ChromaDB 文档管理、MD5 去重、批量/单文件入库 |
| 模型工厂 | `model/factory.py` | ChatModel / Embedding 单例懒加载 |
| 存储层 | `storage/conversation_store.py` | SQLite 用户/会话/消息/登录态持久化 |
| 评测体系 | `evaluate/` | 用例、执行、打分、指标汇总、基线对比 |
| 工具库 | `utils/` | 配置、日志、文件解析、路径、提示词加载 |

### 3.4 核心业务流程

**主流程（用户提问）**：用户输入 → 消息入库并触发自动标题 → 加载最近 30 条历史 → Agent 流式执行（必要时压缩历史）→ 思考/工具调用/回答流式渲染 → 思考过程与回答分别持久化。

**报告生成流程（强制链路）**：识别报告意图 → `get_user_id` 获取用户 ID → `get_current_month`（或用户指定月份）→ 调用 `fill_context_for_report` 触发中间件切换报告提示词 → `fetch_external_data` 获取使用记录 → 生成 Markdown 报告。

**城市定位流程**：`手动选择城市 > IP 定位（高德 /v3/ip）> 默认城市（广州市）`。

**知识入库流程**：文件（TXT/PDF）→ MD5 去重校验 → 解析 → 分块（200/20）→ 向量化入库 → 记录 MD5 → 刷新 BM25 语料缓存。

---

## 4. 功能需求

> 需求编号规则：FR = 功能需求；每个功能点使用唯一编号，便于追溯与验收。

### 4.1 用户认证（FR-01 组）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| FR-01-01 | 用户注册：输入用户名与密码（≥4 位）创建账号；用户名重复时提示「用户名已存在」 | 高 |
| FR-01-02 | 用户登录：用户名 + 密码校验，成功后将登录态写入会话 token 并注入 URL 参数 | 高 |
| FR-01-03 | 密码重置：输入用户名与新密码完成重置，用户不存在时提示 | 高 |
| FR-01-04 | 登录态保持：刷新页面后通过 URL 中的 `session_token` 自动恢复登录态 | 高 |
| FR-01-05 | 登出：销毁服务端会话 token，清空前端会话状态并跳回登录页 | 高 |
| FR-01-06 | 密码安全存储：PBKDF2-SHA256（10 万次迭代 + 随机 salt）加密存储 | 高 |
| FR-01-07 | 会话上限：同一用户最多保留 5 个活跃登录会话，超出自动淘汰最旧 | 中 |
| FR-01-08 | 表单校验：用户名/密码为空、密码长度 < 4 时给出明确错误提示 | 高 |

### 4.2 智能问答（FR-02 组）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| FR-02-01 | 基于 LangGraph ReAct Agent 执行「思考→行动→观察→再思考」循环回答用户问题 | 高 |
| FR-02-02 | 流式输出：回答按字流式渲染（约 0.015 秒/字），模拟打字效果 | 高 |
| FR-02-03 | 思考过程可视化：Agent 思考文本与工具调用结果在可折叠「思考过程」面板中展示，工具结果超 200 字自动截断提示 | 高 |
| FR-02-04 | 上下文管理：单轮最多携带 20 条历史消息，超出的旧消息每 10 条由 LLM 压缩为一条摘要（≤150 字，保留身份/偏好/诉求/决策/待办） | 高 |
| FR-02-05 | 工具调用约束：Agent 需先输出自然语言思考过程再调用工具；入参必须与工具定义一致（纯文本、无引号符号） | 高 |
| FR-02-06 | 信息不足处理：5 次工具调用后仍无法回答时回复「我不知道」 | 中 |
| FR-02-07 | 动态提示词切换：报告生成场景自动切换为报告写手提示词，其余场景使用主客服提示词 | 高 |
| FR-02-08 | 用户输入为空时不做处理 | 中 |

### 4.3 工具功能（FR-03 组）

系统提供 7 个工具，供 Agent 按需调用：

| 编号 | 工具名 | 入参 | 功能 | 约束 |
|---|---|---|---|---|
| FR-03-01 | `rag_search` | `query`（检索词） | 从知识库检索维保专业资料，返回原始参考资料文本（含来源标注），由 Agent 自行综合 | 必须传纯文本 query |
| FR-03-02 | `get_weather` | `city`（城市名） | 查询指定城市实时天气（天气/温度/湿度/风向/风力/发布时间） | 城市经高德地理编码解析为 adcode 后查询 |
| FR-03-03 | `get_user_location` | 无 | 返回当前用户所处城市（从 runtime context 按次读取） | 涉及用户城市问题必须实时调用，禁止用历史城市名替代 |
| FR-03-04 | `get_user_id` | 无 | 返回当前登录用户 ID | 无 |
| FR-03-05 | `get_current_month` | 无 | 返回当前月份，格式 `YYYY-MM` | 用户已指定月份时不应调用 |
| FR-03-06 | `fetch_external_data` | `user_id`, `month` | 获取指定用户指定月份使用记录（特征/效率/耗材/对比）；外部数据缺失时返回 demo 兜底数据 | 需同时传 user_id 与 `YYYY-MM` 格式 month |
| FR-03-07 | `fill_context_for_report` | 无 | 报告场景触发信号：调用后中间件注入 `report=True` 上下文并切换提示词 | 仅报告生成场景可调用，非报告场景严禁调用；为 `fetch_external_data` 的前置工具 |

### 4.4 工具调用可靠性（FR-04 组）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| FR-04-01 | 工具调用重试：失败后指数退避重试，最多重试 3 次（共 4 次尝试），退避公式 `1.0 × 2^attempt + 随机抖动(0~0.5s)` | 高 |
| FR-04-02 | 错误分类：瞬时错误（网络超时/DNS/服务端 5xx）标记为 `TransientAPIError` 触发重试；永久错误直接抛出 | 高 |
| FR-04-03 | 工具监控日志：每次工具调用记录工具名、入参、成功/失败、重试次数 | 中 |
| FR-04-04 | 评测模式：开启 trace collector 时记录每次工具调用的完整轨迹（工具、参数、成败、次数） | 中 |

### 4.5 RAG 知识检索（FR-05 组）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| FR-05-01 | 文档加载：支持 TXT（UTF-8）与 PDF 两种格式，启动时自动加载 `data/` 目录全部知识文件 | 高 |
| FR-05-02 | 文本分块：`RecursiveCharacterTextSplitter`，块大小 200 字符、重叠 20 字符，多级分隔符（段落/换行/标点） | 高 |
| FR-05-03 | 向量化检索：BGE small zh 模型编码，ChromaDB 向量召回 top 10 | 高 |
| FR-05-04 | 关键词检索：BM25 召回 top 10，中文按字符切分、英文/数字保持连续 | 高 |
| FR-05-05 | 融合策略：RRF 融合（k=60，向量/关键词权重各 0.5），取 top 5；用内容前缀去重 | 高 |
| FR-05-06 | 重排序：Cross-encoder（bge-reranker-v2-m3）对候选重新打分排序取 top 5；模型未缓存或加载失败时自动降级为纯混合检索 | 中 |
| FR-05-07 | 语料缓存：BM25 语料序列化至 `storage/bm25_corpus.pkl`，以文档总数校验，避免每次启动重建 | 中 |
| FR-05-08 | 检索与生成解耦：检索工具只返回原文参考资料，不在工具内部调用 LLM 总结 | 高 |
| FR-05-09 | 检索为空时返回「未检索到相关参考资料」 | 高 |
| FR-05-10 | 语料刷新：知识库新增/删除文件后自动重建 BM25 索引与磁盘缓存 | 高 |

### 4.6 城市定位与天气（FR-06 组）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| FR-06-01 | IP 定位：通过客户端真实 IP 调高德 `/v3/ip` 接口解析城市，超时 3 秒 | 中 |
| FR-06-02 | 手动选择：侧边栏提供 15 个常用城市下拉框，选择后立即生效并标注来源「手动选择」 | 高 |
| FR-06-03 | 定位优先级：手动选择 > IP 定位 > 默认城市（广州市） | 高 |
| FR-06-04 | 会话级隔离：城市信息存储于会话级状态与每次执行的 runtime context，不同用户互不影响 | 高 |
| FR-06-05 | 天气查询：城市名 → 地理编码（adcode）→ 天气信息查询；查询失败返回可读的错误提示 | 高 |

### 4.7 报告生成（FR-07 组）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| FR-07-01 | 报告意图识别：识别「生成/查询个人使用报告」类请求并进入报告链路 | 高 |
| FR-07-02 | 报告数据获取：真实数据源为 `data/external/records.csv`（字段：用户ID、特征、效率、耗材、对比、月份）；该用户/月份无数据时生成确定性 demo 记录兜底 | 高 |
| FR-07-03 | 报告格式：按报告提示词生成中文 Markdown 报告，标题为「扫地机器人使用情况报告与保养建议」，不得直接输出检索原文，需结合数据给出具体建议 | 高 |
| FR-07-04 | 月份处理：用户未指定月份时调用 `get_current_month` 获取当月 | 高 |

### 4.8 会话管理（FR-08 组）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| FR-08-01 | 会话列表：按更新时间倒序展示最近 20 个会话，支持切换、删除、新建 | 高 |
| FR-08-02 | 自动标题：首条消息触发后台线程用 LLM 生成 ≤15 字标题（超长截断），失败回退取前 10 字 | 中 |
| FR-08-03 | 消息持久化：用户消息与客服消息均入库；客服消息区分存储原始内容（含思考/工具输出）、干净回答、思考过程 HTML | 高 |
| FR-08-04 | 历史上下文取干净回答：加载最近 30 条消息时，客服消息优先取 `answer` 字段，避免工具输出污染模型上下文 | 高 |
| FR-08-05 | 会话隔离：所有会话操作均携带 `user_id` 校验，用户仅能查看/删除自己的会话 | 高 |
| FR-08-06 | 历史消息渲染：切换会话时恢复历史消息，思考过程以折叠面板还原 | 高 |

### 4.9 知识库管理（FR-09 组）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| FR-09-01 | 启动自动加载：知识库为空或存在新文件时自动执行加载，首次约需 1-2 分钟，页面显示加载进度提示 | 高 |
| FR-09-02 | 页面上传：支持 TXT/PDF 上传入库，入库成功/已存在/失败均有明确反馈（toast + 提示条） | 高 |
| FR-09-03 | MD5 去重：文件入库前计算 MD5（4KB 分片读取），已入库文件跳过 | 高 |
| FR-09-04 | 文件删除：删除文件同时清除 ChromaDB 中对应文档块（按 source 路径）与 MD5 记录，并刷新 BM25 语料 | 高 |
| FR-09-05 | 加载失败文件管理：解析失败的文件单独列出，支持删除后重新上传 | 中 |
| FR-09-06 | 状态展示：侧边栏展示文档块总数、已加载文件列表、加载失败列表 | 中 |

### 4.10 评测体系（FR-10 组）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| FR-10-01 | Agent 全链路评测：批量执行 30 条测试用例（7 类场景：天气保养 7 条、保养咨询 5 条、报告生成 6 条、故障排查 4 条、选购建议 3 条、多工具协作 2 条、边界情况 3 条），统计通过率、均分、平均工具调用数、平均耗时 | 高 |
| FR-10-02 | 用例判定规则：期望工具子集校验、禁止工具校验、工具调用次数上下限、期望关键词、禁止关键词 | 高 |
| FR-10-03 | LLM-as-judge 打分：独立裁判提示词，从完整性、准确性、实用性、无幻觉 4 个维度（1-5 分）打分，输出 overall 与评分理由；回答含拒答话术或与问题无关时 overall 直接为 0；judge 使用固定 prompt 与低温保证一致性 | 高 |
| FR-10-04 | 失败根因分类：execution_error / empty_response / rag_empty / off_topic / judge_zero / tool_failure / missing_tool / wrong_tool / too_many_calls / missing_keyword / forbidden_keyword | 高 |
| FR-10-05 | RAG 独立评测：20 条标注数据集（query → 应命中文件名前缀），计算 Recall@5 与 Precision@5，与 Agent 能力解耦评估 | 高 |
| FR-10-06 | 基线管理：支持 `--baseline` 将当次结果设为基线；每次运行自动与基线对比并输出差异报告（通过率/均分/分场景/失败分布） | 中 |
| FR-10-07 | 结果导出：评测结果写入 CSV（UTF-8 BOM），包含用例、实际工具序列、评分、通过状态、失败原因、耗时 | 中 |
| FR-10-08 | 运行方式：`python -m evaluate.run [--baseline] [--rag-only]` | 中 |

### 4.11 系统监控与日志（FR-11 组）

| 编号 | 需求描述 | 优先级 |
|---|---|---|
| FR-11-01 | 日志输出：控制台 INFO 级 + 文件 DEBUG 级，格式含时间/模块/级别/文件行号 | 中 |
| FR-11-02 | 日志轮转：单文件最大 10MB，保留最近 10 个文件 | 中 |
| FR-11-03 | 关键节点埋点：工具执行、模型调用前（消息数量与末条消息类型）、语料刷新、模型加载等 | 中 |

---

## 5. 数据需求

### 5.1 数据库设计（SQLite，WAL 模式，外键开启）

**users（用户表）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | TEXT PK | 用户 ID（`u` + 时间戳 + 随机串） |
| username | TEXT UNIQUE | 用户名 |
| password_hash | TEXT | PBKDF2-SHA256 密码哈希 |
| salt | TEXT | 随机盐（16 字节 hex） |
| created_at | TEXT | 创建时间（UTC ISO8601） |

**conversations（会话表）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | TEXT PK | 会话 ID（时间戳毫秒级） |
| user_id | TEXT FK → users.id | 所属用户（级联删除） |
| title | TEXT | 会话标题（LLM 生成或前 10 字） |
| created_at / updated_at | TEXT | 创建 / 更新时间 |

索引：`(user_id, updated_at DESC)`。

**messages（消息表）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK AUTOINCREMENT | 消息 ID |
| conversation_id | TEXT FK → conversations.id | 所属会话（级联删除） |
| role | TEXT | `user` / `assistant` |
| content | TEXT | 原始内容（assistant 含思考过程与工具输出） |
| answer | TEXT | 客服干净回答（不含思考过程） |
| thinking_html | TEXT | 思考过程折叠面板 HTML |
| created_at | TEXT | 创建时间 |

索引：`(conversation_id, id)`。

**sessions（登录会话表）**

| 字段 | 类型 | 说明 |
|---|---|---|
| token | TEXT PK | 会话 token（32 字节随机 hex） |
| user_id | TEXT FK → users.id | 所属用户（级联删除） |
| username | TEXT | 用户名冗余 |
| created_at | TEXT | 创建时间 |

索引：`(user_id)`；每用户最多 5 条活跃会话。

### 5.2 知识库数据（文件系统）

- 位置：`data/` 目录，支持 TXT、PDF。
- 已内置知识文件：扫地机器人 100 问、扫拖一体机器人 100 问、维护保养、故障排除、选购指南、性能监控字段对应、短信记录接口、公司保密与安全管理制度等。
- 去重记录：`md5.txt`（每行一个已入库文件的 MD5）。
- 向量持久化：`chroma_db/`（ChromaDB 数据 + SQLite 元数据）。
- 语料缓存：`storage/bm25_corpus.pkl`。

### 5.3 外部数据（报告数据源）

- 位置：`data/external/records.csv`（路径可在 `config/agent.yml` 的 `external_data_path` 配置）。
- 字段：用户 ID、特征、效率、耗材、对比、月份。
- 缺失兜底：目标用户/月份无记录时，按 `hash(user_id + month)` 确定性生成 demo 记录（清扫覆盖率、效率、边刷寿命、机型对比等）。

### 5.4 配置文件

| 文件 | 主要配置项 |
|---|---|
| `config/rag.yml` | chat_model_name（deepseek-v4-flash）、embedding_model_name（bge-small-zh-v1.5）、base_url |
| `config/agent.yml` | gaodekey、gaode_base_url、gaode_timeout、default_city（广州市）、external_data_path、public_ip_sources |
| `config/chroma.yml` | collection_name、persist_directory、k、data_path、md5_hex_store、allow_knowledge_file_type、chunk_size（200）、chunk_overlap（20）、separators |
| `config/prompts.yml` | main_prompt_path、report_prompt_path |
| `.env` | DEEPSEEK_API_KEY、GAODE_KEY |

---

## 6. 外部接口需求

### 6.1 DeepSeek LLM API（OpenAI 兼容）

| 项目 | 内容 |
|---|---|
| 协议 | HTTPS / OpenAI Chat Completions 兼容 |
| Base URL | `https://api.deepseek.com` |
| 模型 | `deepseek-v4-flash` |
| 鉴权 | Header `Authorization: Bearer DEEPSEEK_API_KEY` |
| 调用配置 | 超时 60 秒、max_retries 3 |
| 用途 | Agent 主模型、标题生成、对话摘要、LLM-as-judge 裁判 |

### 6.2 高德地图 API

| 接口 | 用途 | 关键参数 |
|---|---|---|
| `/v3/ip` | IP 定位解析城市 | key、ip |
| `/v3/geocode/geo` | 城市名 → adcode 地理编码 | key、address |
| `/v3/weather/weatherInfo` | 天气查询 | key、city(adcode)、extensions=base |

- 鉴权：`GAODE_KEY` 环境变量或 `config/agent.yml` 的 `gaodekey`。
- 超时 5 秒；5xx/网络错误抛 `TransientAPIError` 触发中间件重试。

### 6.3 HuggingFace 模型

| 模型 | 用途 | 加载方式 |
|---|---|---|
| BAAI/bge-small-zh-v1.5 | Embedding | 本地缓存离线加载（`local_files_only=True`），默认 `HF_HUB_OFFLINE=1` |
| BAAI/bge-reranker-v2-m3 | 重排序 | 延迟加载，仅本地已缓存时启用；下载源可配置 `HF_ENDPOINT=https://hf-mirror.com` |

### 6.4 环境变量

| 变量 | 必填 | 说明 |
|---|---|---|
| `DEEPSEEK_API_KEY` | 是 | DeepSeek API 密钥 |
| `GAODE_KEY` | 是（天气/定位功能） | 高德地图 Web 服务 Key |

---

## 7. 非功能需求

### 7.1 性能需求

| 编号 | 需求 | 指标 |
|---|---|---|
| NFR-01 | 启动性能 | 懒加载：Embedding/重排序/LLM 均延迟初始化；Reranker 未缓存时不阻塞启动 |
| NFR-02 | 检索性能 | BM25 语料缓存命中时秒级完成混合检索与重排 |
| NFR-03 | 响应体验 | 回答流式输出，首包感知延迟低；耗时操作（上传/删除/入库）均带 loading 反馈 |
| NFR-04 | 上下文窗口控制 | 单轮模型输入最多 20 条历史 + 1 条摘要，控制 token 消耗 |

### 7.2 安全需求

| 编号 | 需求 |
|---|---|
| NFR-05 | 密码不得明文存储，使用 PBKDF2-SHA256（10 万次迭代 + 随机盐） |
| NFR-06 | 所有 SQL 使用参数化查询，防止 SQL 注入 |
| NFR-07 | 会话数据按 `user_id` 隔离，删除操作带双条件（id + user_id）校验 |
| NFR-08 | 前端渲染用户/模型文本前进行 HTML 转义，防止 XSS |
| NFR-09 | 登录 token 使用密码学安全随机数（`secrets` 模块）生成 |
| NFR-10 | API 密钥不写入代码与镜像，通过环境变量注入 |

### 7.3 可靠性需求

| 编号 | 需求 |
|---|---|
| NFR-11 | 工具调用瞬时失败自动重试（最多 4 次尝试），重试带指数退避与抖动避免雪崩 |
| NFR-12 | 重排序模型不可用自动降级为混合检索，不影响主链路 |
| NFR-13 | 天气/定位外部服务不可用时返回友好提示，不中断对话 |
| NFR-14 | 单文件解析失败不影响其余文件入库，失败文件可管理 |
| NFR-15 | 评测单用例异常不中断整批执行，异常独立记录 |
| NFR-16 | 知识库文件入库失败时回滚 MD5 记录，避免脏状态 |

### 7.4 可维护性需求

| 编号 | 需求 |
|---|---|
| NFR-17 | 模块化分层架构，各层职责单一、通过接口解耦 |
| NFR-18 | 关键参数（模型名、分块大小、检索数、城市、超时等）均 YAML 配置化 |
| NFR-19 | 统一日志体系（控制台 + 文件轮转），关键节点可追踪 |
| NFR-20 | 评测体系可持续度量质量，基线对比反映变更影响 |

### 7.5 可用性与部署

| 编号 | 需求 |
|---|---|
| NFR-21 | Docker 单容器部署，Dockerfile 预下载模型，Compose 一键编排（端口 8501） |
| NFR-22 | 持久化卷挂载：`chroma_db`、`logs`、`storage`、`data` 四目录 |
| NFR-23 | 容器 `restart: unless-stopped`，异常退出自动重启 |
| NFR-24 | 知识库数据可在容器外直接维护（挂载 `data` 目录） |

---

## 8. 验收标准

| 编号 | 验收项 | 标准 |
|---|---|---|
| AC-01 | 用户认证 | 注册/登录/重置/登出全流程可用，密码加密存储，多用户数据互不可见 |
| AC-02 | 智能问答 | 30 条评测用例通过率 ≥ 90%，judge 均分 ≥ 4.0/5（基线为参照） |
| AC-03 | RAG 检索 | 20 条标注数据 Recall@5 ≥ 0.8 |
| AC-04 | 报告生成 | 报告用例工具链路完整（ID→月份→fill_context→fetch），输出符合报告模板 |
| AC-05 | 知识库管理 | 上传/删除/去重/失败管理全流程可用，操作有明确反馈 |
| AC-06 | 会话管理 | 新建/切换/删除/自动标题/历史恢复可用，数据按用户隔离 |
| AC-07 | 部署 | Docker 构建成功，容器启动后功能可用，卷挂载持久化生效 |
| AC-08 | 稳定性 | 外部服务不可用时系统不崩溃，优雅降级 |

---

## 9. 附录：评测用例场景分布

| 场景 | 数量 | 代表用例 |
|---|---|---|
| 天气保养 | 7 | 结合城市天气保养、回南天注意事项、高温电池影响 |
| 保养咨询 | 5 | 滤网更换周期、边刷缠发、拖布发臭 |
| 报告生成 | 6 | 专属使用报告、指定月份报告、非正式报告请求 |
| 故障排查 | 4 | 不充电、迷路转圈、吸力下降、异常噪音 |
| 选购建议 | 3 | 户型+宠物、地毯场景、预算导向 |
| 多工具协作 | 2 | 耗材更换周期综合评估、环境参数调优 |
| 边界情况 | 3 | 纯闲聊零工具、非法城市、空输入 |

---

*本文档由代码现状整理生成，与系统当前实现保持一致；后续需求变更需同步更新本文档并记录版本。*
